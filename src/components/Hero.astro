<section class="hero">
    <p class="hero__title">~ My memories, my journey ~</p>
    <p class="hero__description">
        Welcome! I share my learning journey, images, beside my other <a href="https://placetohidee.blogspot.com" target="_blank">cute blog</a>.
    </p>
    <div class="hero__quote">
        <p class="quote">
            <span class="quote__head"
                >&#x201CGood morning, and in case I don't see ya,</span
            >
            <span class="quote__tail">
                good afternoon, good evening, and good night.&#x201D</span
            >
        </p>
        <p class="quote__author">&mdash; Truman</p>
    </div>
    <div class="hero__music">
        <button class="music__btn" id="playBtn" aria-label="Play">></button>
        <input
            type="range"
            id="progress"
            value="0"
            step="0.1"
            min="0"
            class="music__progress"
        />
        <span class="music__time">
            <span id="current">0</span>
            <span class="music__divider">/</span>
            <span id="duration">0</span>
        </span>
        <audio id="audio" src={`${import.meta.env.BASE_URL}/country-roads-john-denver.mp3`}></audio>
    </div>
</section>

<style>
    .hero {
        padding: var(--spacing-lg) 0;
    }

    .hero__title {
        margin: var(--spacing-md) 0;
        font-size: var(--font-2xl);
        font-weight: 600;
    }

    .hero__description {
        margin-bottom: var(--spacing-md);
        font-weight: 400;
    }

    .hero__quote {
        font-size: var(--font-lg);
        max-width: 45rem;
        margin: 0 auto;
        font-style: italic;
        text-align: center;
    }

    .quote__author {
        text-align: right;
        font-weight: 600;
        font-style: normal;
    }

    .quote {
        display: flex;
        flex-direction: column;
    }

    .quote__head {
        display: inline-block;
        margin-right: auto;
    }

    .quote__tail {
        display: inline-block;
        margin-left: auto;
        margin-bottom: var(--spacing-xs);
    }

    @media (max-width: 768px) {
        .hero__title {
            font-size: var(--font-xl);
        }

        .hero__quote {
            font-size: var(--font-md);
        }
    }

    .hero__music {
        margin-top: var(--spacing-md);
        display: flex;
        align-items: center;
        gap: var(--spacing-sm);
        justify-content: center;
        width: 100%;
        max-width: var(--width-tablet);
        margin-inline: auto;
    }

    .music__btn {
        width: 34px;
        height: 34px;
        background: transparent;
        border: 1px solid var(--color-text);
        cursor: pointer;
    }

    .music__progress {
        flex: 1;
        height: 5px;
        appearance: none;
        background: var(--color-code);
        border-radius: 999px;
        cursor: pointer;
    }

    .music__progress::-webkit-slider-thumb {
        appearance: none;
        width: 12px;
        height: 12px;
        background: var(--color-text);
        border-radius: 50%;
        border: none;
    }

    .music__progress::-moz-range-thumb {
        width: 12px;
        height: 12px;
        background: var(--color-text);
        border-radius: 50%;
        border: none;
    }
</style>

<script>
    import "../scripts/music-player.js";
</script>